Place the signed Android release here as Vyamit-v1.apk (or another .apk filename).
Run `npm run build` to regenerate the release manifest. The website will then automatically show the Android download option and its file size.
